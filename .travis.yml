language: python
python:
  - 3.6
  - pypi3
install:
  - python3 setup.py install
script:
  - pytest
before_deploy:
  - python3 setup.py sdist
deploy:
  provider: pypi
  user: "PYPI_USER"
  password:
    secure: "PYPI_PASSWORD_SECURE"
  on:
    all_branches: true